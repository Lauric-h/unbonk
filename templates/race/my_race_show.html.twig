{% extends 'base.html.twig' %}

{% block title %}Mes courses importées{% endblock %}

{% block body %}
    {{ race.name }}
    <form action="{{ url('app.nutrition_plan.create', {runnerRaceId: race.id}) }}" method="post">
        <button>Créer un NP</button>
    </form>
{% endblock %}