{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create Brand</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button type="submit">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}

